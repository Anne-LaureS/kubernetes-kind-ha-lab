apiVersion: apps/v1
kind: Deployment
metadata:
  name: demo-v1
spec:
  replicas: 1
  selector:
    matchLabels:
      app: demo-v1
  template:
    metadata:
      labels:
        app: demo-v1
    spec:
      containers:
      - name: demo
        image: demo:v1
        ports:
        - containerPort: 80
---
apiVersion: v1
kind: Service
metadata:
  name: demo-v1
spec:
  selector:
    app: demo-v1
  ports:
  - port: 80
    targetPort: 80
