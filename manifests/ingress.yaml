apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: demo
spec:
  ingressClassName: nginx
  rules:
  - host: demo.local
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: demo
            port:
              number: 80

  - host: v1.local
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: demo-v1
            port:
              number: 80
